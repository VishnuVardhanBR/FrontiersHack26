{
  "name": "quizcraft",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run build:shared && npm run typecheck --workspaces --if-present",
    "build:shared": "npm run build --workspace @quizcraft/shared",
    "dev:server": "npm run build:shared && npm run dev --workspace @quizcraft/server",
    "dev:bot": "npm run build:shared && npm run dev --workspace @quizcraft/bot",
    "dev:frontend": "npm run dev --workspace @quizcraft/frontend",
    "setup:mc": "./mc-server/setup.sh",
    "start:mc": "./mc-server/start.sh",
    "run": "./run.sh",
    "validate": "./validate.sh"
  }
}
